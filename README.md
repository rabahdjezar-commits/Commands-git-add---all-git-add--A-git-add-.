# Commands-git-add---all-git-add--A-git-add-.
Commands: git add --all, git add -A, git add . · Effect: Stage the changes within the current directory you're in · These same three commands are shown as having a different scope when executed from within a subdirectory
